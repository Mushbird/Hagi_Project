var 이름 = 'kim';
var 이름리스트 = ['kim']; // String
var 오브젝트이름 = { name: 'type' }; // ? 은 name속성은 옵션이에요! 라고 알려주는 것
var 이름및넘버 = 123;
var 타입설정된이름 = 123; // 타입을 지정할 수도 있다.
function 함수(x) {
    return x * 2;
}
var john = [123, false];
var johnObject = { name: 'kim' };
var johnAllObject = { name: 'kim', age: '123' };
var User = /** @class */ (function () {
    function User(name) {
        this.name = name;
    }
    return User;
}());
